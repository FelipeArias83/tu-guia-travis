
lenguaje: node_js
node_js: 
    - "13.12.0"

before_install: cd ./tu-guia-travis
    
cache:
    directories: 
        - ./tu-guia-travis/node_modules
install:
     - npm install
 script:
    - npm run test:ci -- --watch=false --no-progress --browsers=ChromeHeadlessNoSandbox 
    - npm run-script build
    — npm install -g ionic
       
    deploy:
        provider:
         skip_cleanup: true:
        github_token: $GITHUB_TOKEN
        local_dir: www
        on:
            branch: master


       
                